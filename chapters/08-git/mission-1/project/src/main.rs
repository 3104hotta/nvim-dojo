// Mission 1: nvim-internal git workflow.
// Add `println!("Hello, nvim!");` inside `fn main` and commit it via :!git and :terminal.

fn main() {
    let app_name = "ch08-mission1";
    let _ = app_name;
}
