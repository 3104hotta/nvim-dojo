-- Mission 3 — built-in LSP
-- Add the LSP setup listed in README.md.

-- 1. LspAttach autocmd: buffer-local keymaps for gd/gr/K/<leader>rn/<leader>ca/]d/[d/<leader>f

-- 2. BufReadPre autocmd for *.rs: vim.lsp.start({ name, cmd, root_dir })

-- 3. vim.diagnostic.config({ virtual_text, signs, underline })
